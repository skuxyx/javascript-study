// 주석

/**
 * 변수 규칙
 * 1. 라틴문자(0 ~ 9, a ~ z, A ~ Z ), _
 * 2. 대소문자를 구분함.
 * 3. 변수 이름을 작성할 때 두번째 단어는 항상 대문자로. camelCase ( likeThis )
 * 4. 한국어 X
 * 5. 예약어 X
 * 6. 숫자로 시작 X
 * 7. 특수문자 사용 X ( _ , $ 두가지는 예외 )
 * 8. 최대한 의미있게, 구체적인 이름으로 작성.
 */

let apple;
let redApple;

// 올바르지 않은 예제.
let number = 20;
let audio1;
let audio2;

// 좋은 예제.
let myAge = 20;
let backgroundAudio;
let windAudio;

// 꿀팁 ( audio를 먼저 작성하면 찾기 쉬움 )
let audioBackground;
let audioWind;
