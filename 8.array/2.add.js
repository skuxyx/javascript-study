const dog = ['retriever', 'bichon', 'pomeranian'];

// 배열은 0 부터 시작.
console.log(dog[0]);
console.log(dog[1]);
console.log(dog[2]);

for (let i = 0; i < dog.length; i++) {
  console.log(dog[i]);
}

// 배열에 몇개가 들어있는지.
console.log(dog.length); // console.log => 3
